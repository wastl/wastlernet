INCLUDE(FindProtobuf)
FIND_PACKAGE(Protobuf REQUIRED)
INCLUDE_DIRECTORIES(${PROTOBUF_INCLUDE_DIRS})
PROTOBUF_GENERATE_CPP(PROTO_SRC PROTO_HEADER devices.proto)

ADD_LIBRARY(devices ${PROTO_HEADER} ${PROTO_SRC} )
TARGET_LINK_LIBRARIES(devices PUBLIC ${Protobuf_LIBRARIES})
